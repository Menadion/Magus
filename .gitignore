.gradle/
build/
app/build/
local.properties
.idea/
*.iml
.kotlin/
captures/
# Firebase config for this project; keep it out of any public repo
app/google-services.json

# Release signing: path + password to the key outside the repo
keystore.properties

# Screenshots for the design pass; show real locations, never public
design/
